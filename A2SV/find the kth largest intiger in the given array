class Solution(object):
    def kthLargestNumber(self, nums, k):
        """
        :type nums: List[str]
        :type k: int
        :rtype: str
        """
        coll=[]
        for i in nums:
            coll.append(int(i))
        coll.sort()
        return str(coll[len(nums)-k])
